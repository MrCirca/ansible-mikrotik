DEFAULT_DEVICE_GROUP = {
    'name': 'default',
    'devices': [
    {% for client in clients_router %}
      {% for ip in client %}
        {"name":"{{client[ip]}}", "ip":"{{ip}}"}{% endfor %}{% if not loop.last %},{% endif %}

    {% endfor %}

    ],
    'compare_devices': [] 
}
